{
  "buildCommand": "flutter build web",
  "outputDirectory": "build/web",
  "devCommand": "flutter run -d web-server --web-port=$PORT",
  "installCommand": "if [ ! -d flutter ]; then git clone https://github.com/flutter/flutter.git -b stable --depth 1; fi && flutter/bin/flutter doctor",
  "framework": null,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
